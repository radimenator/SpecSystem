links:
  roam:
    project_page: "PO/..."

  local_paths:
    project_root: "~/Projects/<project>"
    spec_root: "~/SpecSystem/projects/<project>"

  repositories:
    - url: https://github.com/...
      description: hlavní repo

  services:
    - name: VPS
      type: infra
      provider: Hetzner
